.First<-function(){
  #建立自動執行
  #啟動
  options(prompt="$ ", continue="+\t")  # $ is the prompt
  par(pch = "+")                        # plotting character
  setwd("F:/GitHub/getData/R")
  library(xlsx)
  library(dplyr)
  cat("\n 歡迎!!! \n 已經為您載入: \n xlsx dplyr \n",date())
}
.First()
