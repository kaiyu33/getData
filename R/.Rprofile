.First<-function(){
  #建立自動執行
  #啟動
  setwd("F:/GitHub/getData/R")
  library(xlsx)
  library(dplyr)
  cat("\n 歡迎!!! \n 已經為您載入: \n xlsx dplyr \n",format(Sys.time(), "%x %A %X \n ( %Z %z )"))
}
.First()
