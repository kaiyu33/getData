.First<-function(){
  #建立自動執行
  #啟動
  setwd("F:/GitHub/getData/R")
  # library(xlsx)
  # library(quantmod)
  library(dplyr)
  cat("\n Welcome!!! \n import: \n dplyr \n",format(Sys.time(), "%x %A %X \n ( %Z %z )"))
}

